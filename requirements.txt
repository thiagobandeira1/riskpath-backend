# Pinned to versions known to work with the V17-validated XGBoost model
# copied from the publication repo on 2026-05-17.
#
# Web app is XGBoost-only by design. LightGBM and CatBoost are intentionally
# excluded from the dependency tree to keep the install slim.

# Core
pandas==2.3.3
# numpy bumped from 1.26.4 to 2.1.3 to satisfy shap==0.51.0 (requires numpy>=2);
# 2.1.3 is also compatible with scikit-learn 1.8.0, xgboost 3.2.0, and pandas 2.3.3.
numpy==2.1.3
pyarrow==23.0.1
joblib==1.4.2

# Modelling
scikit-learn==1.8.0
xgboost==3.2.0

# Interpretability
# shap>=0.51 required for compatibility with xgboost 3.2.0 (older SHAP
# versions fail to parse XGBoost's bracketed base_score field).
shap==0.51.0

# Web app stack (uncomment what you choose to build with)
# streamlit>=1.40
# fastapi>=0.115
# uvicorn[standard]>=0.32
# pydantic>=2.9
# plotly>=5.24
